#include "main.h"
/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("Infinit lopp incoming :(\m");

	i = 0;

	/**
	 * while (i < 10)
	 * {
	 * 	putchar(i);
	 * }
	 */
	print("Infinit loop avoided! \\o/\n");

	return (0);
}

